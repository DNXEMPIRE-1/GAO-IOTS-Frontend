export enum FieldTypes {String ,
  Boolean,
  DataTime,
  Number,
  Selections
};

export enum SmtpAuthMethod {
   NONE,
   PASSWORD,
   ENCRYPTED_PASSWORD,
   KERBEROS,
   NTLM
}

export enum SmtpProtocol {
  NONE,
  STARTTLS,
  SSL
}
