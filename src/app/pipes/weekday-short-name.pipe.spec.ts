import { WeekdayShortNamePipe } from './weekday-short-name.pipe';

describe('WeekdayShortNamePipe', () => {
  it('create an instance', () => {
    const pipe = new WeekdayShortNamePipe();
    expect(pipe).toBeTruthy();
  });
});
