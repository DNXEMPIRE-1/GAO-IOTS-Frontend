import { TypeofpipePipe } from './typeofpipe.pipe';

describe('TypeofpipePipe', () => {
  it('create an instance', () => {
    const pipe = new TypeofpipePipe();
    expect(pipe).toBeTruthy();
  });
});
