import { ControlTypeDisplayPipe } from './control-type-display.pipe';

describe('ControlTypeDisplayPipe', () => {
  it('create an instance', () => {
    const pipe = new ControlTypeDisplayPipe();
    expect(pipe).toBeTruthy();
  });
});
