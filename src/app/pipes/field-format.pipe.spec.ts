import { FieldFormatPipe } from './field-format.pipe';

describe('FieldFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new FieldFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
